{
  "scripts": {
    "build": "npx ng build --configuration production",
    "dev": "npx ng build --configuration development",
    "serve-ng": "npx ng serve",
    "serve-rest": "node sampleServer.mjs"
  },
  "devDependencies": {
    "express": "4.18.2",
    "solar-lib": "file:../dist/solar-lib"
  }
}
